# Nixpacks configuration for NΞØ Backend
# Resolve UndefinedVar: $NIXPACKS_PATH by explicitly defining the setup phase.

[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[start]
cmd = "pnpm start"
